# Evaluation guide

Use computational sensors (schemas, tests, typechecks, policies, invariants, citation checks) and inferential sensors (quality, research, security, independent reviewer). The API now registers datasets and cases and carries case IDs into replayable run evidence. Keep the generating agent separate from the evaluator. Store grader evidence, model/version, intermediate steps, and replay links. A release is blocked by regression failures or missing verification evidence.
