# Evaluation guide

Use computational sensors (schemas, tests, typechecks, policies, invariants, citation checks) and inferential sensors (quality, research, security, independent reviewer). Keep the generating agent separate from the evaluator. Store datasets, cases, grader evidence, model/version, intermediate steps, and replay links. A release is blocked by regression failures or missing verification evidence.
