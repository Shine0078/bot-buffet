# Privacy and retention

Default local storage is project-scoped and private. Traces and tool outputs are redacted before persistence; providers receive only the context selected by policy. Configure retention by namespace, expire stale memories, and honor export/deletion across relational, object, vector, and audit stores. Audit evidence may require a separate legally approved retention schedule.
